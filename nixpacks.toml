[phases.setup]
nixPkgs = ["python3"]

[phases.install]
cmds = [
    "pip install -r requirements.txt"
]

[phases.build]
cmds = [
    "echo 'Using pre-built frontend SPA from repository (frontend/dist)'"
]

[start]
cmd = "uvicorn api:app --host 0.0.0.0 --port $PORT"
